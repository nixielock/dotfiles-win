theme = "kanagawa-dragon"

[editor]
line-number = "relative"
mouse = false
bufferline = "multiple"
shell = ["pwsh","-c"]
end-of-line-diagnostics = "hint"

[editor.soft-wrap]
enable = true
max-wrap = 30
wrap-indicator = ""

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[editor.smart-tab]
enable = true

[editor.file-picker]
hidden = false

[editor.statusline]
mode.normal = "   NORMAL   "
mode.insert = "-- INSERT --"
mode.select = ">> SELECT <<"
left = ["mode","read-only-indicator","register","selections","spinner","diagnostics"]
center = ["primary-selection-length","spacer","spacer","spacer","spacer","position","separator","spacer","position-percentage"]
right = ["file-modification-indicator","file-name","separator","file-encoding","file-line-ending"]

[editor.inline-diagnostics]
cursor-line = "error"
other-lines = "error"

[keys.normal]
  # tweaks
a = ["append_mode", "collapse_selection"]
i = ["insert_mode", "collapse_selection"]
C-q = "record_macro"
q = "replay_macro"
  # multiline select (replaces C)
C-s = "copy_selection_on_next_line"
  # line selections
x = "extend_line_below"
X = "extend_line_above"
C-x = "extend_to_line_bounds"
  # move via subwords
A-w = "move_next_sub_word_start"
A-e = "move_next_sub_word_end"
A-b = "move_prev_sub_word_start"
  # match inner subword (if not at start)
A-q = ["move_prev_sub_word_start","move_next_sub_word_end"]
  # select to end/start of line
L = "extend_to_line_end"
H = "extend_to_first_nonwhitespace"
  # delete line
D = ["extend_to_line_bounds", "delete_selection"] 
  # change line
C = ["extend_to_line_bounds", "change_selection"]
  # move line up/down
C-k = ["extend_to_line_bounds", "delete_selection", "move_line_up", "paste_before"] 
C-j = ["extend_to_line_bounds", "delete_selection", "paste_after"]
  # advanced moves
# M = []

[keys.insert]
k = { j = "normal_mode" }
C-q = "collapse_selection"
